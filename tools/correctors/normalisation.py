import nibabel as nib
import os

from tools.auxiliary.utils import set_new_data


def median_normalisation(input_image_pfi, input_mask_pfi, output_pfi):
    # TODO: normalisation step needs to be done when creating the template
    # having available all the brain mask for all the subjects.

    pass